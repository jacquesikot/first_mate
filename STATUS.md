# First Mate — Project Status

> Living hand-off log. Every session reads this first and updates it before ending.
> Protocol: see `CLAUDE.md`. Spec: see `PRD.md`.

**Current phase:** Phase 3 — web dashboard: **built and live-verified end to end**, with Jacques's hands-on feedback addressed (2026-08-19: task-first scoping sessions, markdown rendering, responsive/no-horizontal-scroll, and explicit per-task starting points). 107 daemon tests + 13 dashboard tests pass; phase-2 smoke passes; a full picker → starting point → session → scoping → approve → worker → `done` run verified live against a deliberately messy repo. Next: Phase 4 — Slack + memory loop.

---

## Next up (start here)

1. **Jacques: re-check the dashboard** — `cd dashboard && pnpm install && pnpm build`, then `cd daemon && uv run fm serve --open`. Round 1 of feedback is done (see the 2026-08-19 "feedback round 1" Done entry). Worth a look specifically at: the new-task → session hand-off, markdown density in the scoping conversation, and narrow-window layout.
2. **Phase 4 — Slack + memory loop** (PRD §6.9, §6.6, §8): socket-mode Slack app (blocking questions with option buttons, completions/failures, threaded per task, `fm answer` equivalence, `remember:` replies, `status/pause/abandon` commands); learning extraction after successful steps; recurring-answer promotion suggestions; memory compaction with archive. Question fingerprinting (PRD §6.5) is not built yet — Phase 4 needs it for promotion suggestions.
3. Nice-to-haves carried forward: a first live interactive run of `fm task "<goal>"` by Jacques (browser scoping is now live-verified; the terminal chat still hasn't had a human), `block-with-timeout` ask mode (park-only today), question batching at step boundaries (PRD §6.5), `--max-budget-usd` per worker, "promote to memory?" surface in the Memory view (needs Phase 4 fingerprinting), repeated-question fingerprinting (observed live: a resumed worker re-asked an equivalent question; a firmer answer settled it — Phase 4's promotion machinery is the real fix).

## In progress

- Nothing half-finished. Phase 3 machinery is complete and verified; only Jacques's visual sign-off is pending.

## Done

- **2026-08-19** — **Every task declares its starting point; the worktree is cut at scoping time (Jacques's feedback round 2).** Previously the worktree was created lazily on first run, off `HEAD`, so a task silently inherited whatever the operator had checked out — wrong when their tree is mid-work, stale, or on an unrelated branch. Now: **`GET /fs/refs?repo=…`** runs `git fetch --prune` (remote-tracking refs only, safe against a dirty tree) and returns ranked candidates with the freshness facts needed to choose knowingly — age, `ahead`/`behind` vs upstream, `gone` upstreams, dirty-checkout flag, short SHA, subject — with `role` marking the remote default and the checked-out branch, and `fm/**` task branches filtered out (they are outputs, not starting points). New `gitops` helpers: `has_remote`, `fetch`, `default_branch` (via `origin/HEAD` → `ls-remote --symref` → conventional names), `current_branch`, `is_dirty`, `list_refs`, `resolve_ref`, `delete_branch`. **`POST /scoping` takes `base`**, resolves it to a SHA, cuts the worktree immediately, and points the scoping conversation's cwd at it — so the assistant reads a clean checkout of the chosen point, not the operator's work in progress (the prompt gains a `WORKDIR_NOTE` explaining this, and still writes `repo` as the canonical repo path). `Task` gains `base`/`base_sha` (pinned, so a run days later starts where the operator decided); the orchestrator's `create_worktree` now passes `base_sha` instead of `HEAD` and remains idempotent, reusing the scoping worktree. `POST /tasks` takes `base` too; an explicit unresolvable ref is a 400, an unresolvable *default* (repo with no commits) is not. Abandoning a scoping task removes its worktree and branch — but only when truly untouched (`discard_unused_worktree` keeps anything with uncommitted files or commits past the base). CLI: `fm task "<goal>" --from REF` / `--no-fetch`, and because the terminal flow has no worktree (it stays contract-first), it warns when the operator's checkout differs from the chosen base and tells the scoping session the same, so nothing is scoped against code the worker will never see. **UI (`dashboard/src/views/StartPoint.tsx`):** a "start from" card between repository and task — radio rows showing `origin/main · remote default · 5h old`, `main · checked out · local · 1 behind origin/main · uncommitted changes here` (amber), recent branches, `N more branches ▾`, an "other branch, tag, or commit…" field, a `↻ refetch` control, a fetch-status line, and one plain sentence: "a fresh worktree is cut from X onto the task's own branch — your checkout is never touched". Task detail shows `started from` (+ SHA) in both the scoping and running sidebars and reads `base → branch` on the worktree card. **Live-verified against a purpose-built messy repo** (local `main` 1 behind origin, a colleague's unfetched commit on origin, an uncommitted syntax error in the working tree, plus an older feature branch): the picker fetched and reported all of it correctly; starting from `origin/main` produced a worktree containing the colleague's commit and none of the local mess; the scoping session read that clean tree; the task ran to `done` with 3/3 criteria on its own branch; and the operator's checkout was byte-for-byte untouched afterwards. 107 daemon tests (+8).
- **2026-08-19** — **Jacques's feedback round 1 on Phase 3 (three items, all fixed and live-verified).**
  1. **Task-first scoping — no more "New task" limbo.** `POST /scoping` now creates the Task immediately in the new `scoping` status (placeholder contract: goal + repo, no steps) and returns `{chat, task}`; the browser navigates straight to `#/task/<id>`. The conversation renders *inside* the task view (new `dashboard/src/views/Scoping.tsx`, surfaced as a `Scoping` tab in `TaskDetail`, with a phase-specific sidebar — repo, branch-on-approval, what-happens-next — instead of the zeroed session/worktree cards). Scoping turns moved **off the request path** (`spawn_chat_turn`): `POST /scoping` and `POST /scoping/{id}/message` return at once (the operator's turn is recorded synchronously so the message + thinking state appear immediately), and the panel follows over WS `{kind:"scoping"}` frames plus a 1.5s poll while a turn is in flight. `POST .../message` accepts `{"wait": true}` for synchronous callers. **Approval adopts the contract into the existing task** (`Store.adopt_contract`) rather than minting a second one — verified live: exactly one task from picker to `done`. Task-level guards: `run` 409s on a `scoping` task, `abandon` closes the chat too (and vice-versa), boot `reconcile` hands a chat stuck in `thinking` back to the operator with a note (verified by killing the daemon mid-turn, then resuming). Scoping tasks appear on Now (own section) and Tasks (own group), and count in the header/nav.
  2. **Markdown rendering.** New dependency-free renderer (`dashboard/src/markdown.tsx`, 13 vitest tests) built from React elements only — never `innerHTML`, so assistant text can't inject markup. Covers headings, bold/italic/strike, inline + fenced code, tables, nested/ordered lists, blockquotes, rules, links. Used for scoping replies, worker questions (`QuestionCard`), handoff briefs, and contract `context`. Three bugs found by testing against real replies: intra-word `_`/`*` were eaten (`scope_in` → "scopein" — now CommonMark-ish flanking), a table immediately after a paragraph line had its header row swallowed by the paragraph, and single newlines collapsed to spaces so `Scope in:` / `Scope out:` ran together (now `<br>`).
  3. **Responsive — the page never scrolls horizontally.** `.content` gets `overflow-x: hidden` as a hard stop, with wide content (diffs, code, tables, tmux panes) scrolling inside its own `.scroll-x` box; shared `.page` shell bounds every view; `.truncate`/`overflowWrap: anywhere` applied along the min-width chains that leaked (chat, header, task rows, contract values, evidence chips, file lists); diff rows wrapped in `min-width: max-content` so `+/-` stripes span the scrolled width; sidebar collapses to a 62px icon rail under 820px and the turn layout stacks under 700px. **Verified with headless Chromium: 80 checks — 5 views + all 5 task tabs at 8 widths from 1600px to 420px, zero horizontal overflow, zero console errors.** One real bug found: the rail footer's inline `display:flex` beat the collapse rule, so it survived at narrow widths (layout moved into CSS).
  Also fixed while in there: **`.fm/` and build caches no longer pollute the Changes view or diff tripwire totals** (`gitops.is_review_noise`; the test task showed 13 "changed files" of which 11 were First Mate's own state), and **`pnpm dev` was missing `/scoping` and `/fs` proxy entries** so the whole new-task flow 404'd in dev. 99 daemon tests (+4), 13 dashboard tests (new `pnpm test`), phase-2 smoke passes.
- **2026-08-19** — **Scoping in the browser + repo-picker start flow (Jacques's first feedback on Phase 3).** New task view is now: pick the repo (suggestion chips from `GET /fs/repos` — recent task repos + shallow scan of ~/code etc. — or a directory-browser modal over `GET /fs/browse`, git repos flagged), type the goal, **Start scoping** → the scoping conversation runs in the dashboard (prototype's chat design: who-column, contract card, approve buttons). Daemon side: `scoping_api.py` — each turn is one headless `claude -p` chained with `--resume` (session id re-captured from each turn's JSON since --resume mints a new one); chat state mirrored to `scoping.json` in the scoping dir (jq-able, rehydrates after daemon restart); endpoints `POST /scoping`, `GET/POST /scoping/{id}[/message|/approve|/abandon]`; approval reuses the exact POST /tasks gate. Contract-JSON paste kept as a secondary path. 94 tests pass (3 new). **Live-verified end to end with real claude:** picker → goal → proposal (the assistant read the repo and pushed back on an untestable ask) → contract with 6 criteria → Approve and run → worker ran, parked twice with questions (answered from the API), task reached `done` with 6/6 criteria passing. **Two live-found fixes:** (1) scoping sessions couldn't write their contract — headless dontAsk denied the Write: needs `--add-dir <scoping-dir>` AND the path rule spelled `Edit(//…/**)` — the CLI accepts `Write(path)` rules but never consults them (docs: permissions "Read and Edit"; both the browser and terminal flows were affected — terminal flow fixed too, it had never been human-run); (2) scoping prompt now forbids step prompts that tell workers to run commands outside their allowlist (caused a needless park).
- **2026-08-19** — **Phase 3 web dashboard built and live-verified.** New `dashboard/` (Vite + React 18 + TS, no other runtime deps): thin client over the daemon API, design tokens/glyphs/typography translated from the agreed prototype. Views: Now, Tasks (grouped board + filters), Task detail (tabs: Steps with generation rail + collapsible handoff briefs + criteria with validation evidence; Contract render + JSON edit between steps; Changes file list + diff viewer; Output live pane; Questions history; sidebar session/worktree/context cards), Inbox (blocking vs batched), Memory (view/edit/append), New task (contract JSON submission — scoping stays in the terminal, PRD §10.1 open). Daemon additions (`server.py`): `GET /tasks/{id}/diff` + `/diff/file` (new `gitops.numstat_files`/`diff_file`), `GET /tasks/{id}/output` (on-demand pane capture), `PUT /tasks/{id}/contract` (validated, rejected while running, step state reconciled), memory endpoints (`GET /memory`, `GET/POST/PUT /memory/{project}`), context meters in `/status` + task detail, a `Manager` live loop broadcasting `{"kind":"live", output, context}` WS frames (1s cadence, only while sockets are connected, dedup on change), static mount of `dashboard/dist` at `/ui` (env override `FM_DASHBOARD_DIST`), `/` redirect, `fm serve --open` opens the SPA. **Bug found by real-browser testing: uvicorn had no WS protocol library — `/ws` 404'd over real HTTP while unit tests passed (in-process transport); fixed by adding `websockets` to daemon deps.** 91 unit tests pass (5 new endpoint tests). **Live verification with real sonnet workers (headless Chromium driving the built SPA):** task created via API → worker ran → `fm ask` parked → question card appeared on Now (WS push) → answered by clicking in the browser → contract amended → gen-2 respawn → criteria validated → done; a second run organically exercised the failure ladder (gen-1 exit 1 → auto-retry with failure context → done); live pane streaming + context meter + worker-slot pips captured mid-run in screenshots; zero browser console errors.
- **2026-08-19** — **Phase 2 scoping + scope guard complete.** New modules: `guard.py` (stdlib-only scope-guard engine: gitignore-ish glob matching, Edit/Write/MultiEdit/NotebookEdit path checks, Bash heuristics for redirections/write commands/dependency installers/git push, tripwire config compilation) and `scoping.py` (`fm task "<goal>"` → interactive claude session primed with a scoping prompt that reads memory+repo, proposes scope/steps/criteria, refuses vague criteria, self-checks with `fm contract check`, writes contract JSON that the CLI submits to POST /tasks on session end). CLI grew `fm _guard` (PreToolUse hook: exit 2 + in-band fm-ask instructions on block; fail-closed on internal errors) and `fm contract check`. Contract grew `tripwires` (per-task overrides) + `tripwire_allow`; config.json grew project-default `tripwires` + `scoping_model`. Answers starting with "allow" to scope_change/approval questions mechanically widen `scope_in`/`tripwire_allow` (path evidence) or disable the named tripwire (non-path evidence). Diff-shaped tripwires (`max_diff_lines`, `max_deleted_lines`) checked orchestrator-side at step boundaries (step stays done; task parks; asked once per task). 86 unit tests pass (42 new). **Live verification:** real headless session blocked mid-run by the generated PreToolUse wiring (in-band message honored, GuardBlock event logged); **phase-2 smoke passed first try** (`uv run fm-smoke --scenario phase2`): block → scope_change with correct evidence → park → allow → scope widened → gen-2 resume → both criteria validated → done.
- **2026-08-19** — **Phase 1 orchestrator core complete.** New modules in `daemon/src/firstmate/`: `models.py` (task/contract/step/question dataclasses + `validate_contract` machine-checkability gate), `store.py` (files under `~/.firstmate/` are source of truth, SQLite is a rebuildable index reindexed on boot), `validation.py` (shell criteria + evidence), `workerfiles.py` (per-worker hook scripts → `fm _event` POSTs to daemon; SessionStart injection), `relay.py` (handoff via resume, lifted from spike), `orchestrator.py` (deterministic per-task loop: wall relay, park mode, failure ladder — never a 3rd attempt, escalation question carries diff + failing checks), `server.py` (FastAPI REST + WebSocket, Manager with global worker-slot semaphore + boot reconciliation of orphaned sessions), `cli.py` (`fm serve/task add/run/status/attach/answer/pause/abandon/remember/ask/_event`), `smoke.py` (e2e). 44 unit tests pass (`uv run --group dev pytest`, hermetic). **Smoke passed twice** (`uv run fm-smoke`): worker execution → validation → `fm ask` park (worker torn down with handoff) → answer → respawn with answer injected → task-boundary validation → `done`. First run also organically exercised the failure ladder (worker wrote to repo root instead of worktree; retry with failure context fixed it) — worker prompt now pins cwd=worktree explicitly.
- **2026-08-19** — **Phase 0 relay spike passed (GO).** `daemon/src/firstmate/spike/relay.py`; run with `cd daemon && uv run fm-spike`. A task deliberately overflowed at a 70k-token wall completed correctly across **7 generations** (criterion: ≥3): per-gen progress 0→2→4→6→8→10→12 items with no continuity loss; SessionStart `additionalContext` injection landed in 7/7 sessions (canary-verified); handoff briefs were specific and correct. Cost ≈ $0.45/generation (sonnet workers).
- **2026-08-19** — **Claude Code facts verified against live CLI (2.1.227) + docs**, superseding PRD §11 snapshot — full list in `daemon/README.md`. Highlights: `--session-id` pins session IDs at spawn; JSON output shape confirmed; PreCompact exit-2 blocks compaction; transcripts at `~/.claude/projects/<munged-cwd>/<sid>.jsonl`; interrupting a worker mid-tool-call then `--resume`-ing for a handoff brief works.
- **2026-08-19** — **`daemon/` package scaffolded** (uv, Python 3.12): four execution-module boundaries per PRD §7 — `exec/tmux.py`, `exec/gitops.py`, `exec/context.py`, `exec/hooks.py` — plus `spawner.py` (headless `claude -p` in tmux windows).
- **2026-08-19** — PRD written and agreed (`PRD.md` v1.0).
- **2026-08-19** — Dashboard UI prototype built (`prototype/dashboard.html`, design decisions in `README.md`).
- **2026-08-19** — Multi-session memory system set up (`CLAUDE.md` + this file).
- **2026-08-19** — Git initialized; public repo: https://github.com/jacquesikot/first_mate.

## Decision log

Decisions made outside the PRD, dated, with rationale. PRD §7 constraints are settled and not repeated here.

- **2026-08-19** — **Files are the source of truth; SQLite is a rebuildable index.** PRD §7 says "SQLite + plain files"; resolving the split: every task/contract/question/event lives as JSON/markdown/jsonl under `~/.firstmate/` (criterion 10), and `firstmate.db` is reindexed from files on every `Store()` construction. Deleting the db or hand-editing a file is always safe.
- **2026-08-19** — **Phase 1 task creation = `fm task add <contract.json>`.** Scoping conversations are Phase 2; until then contracts are hand-written JSON, validated by the same machine-checkability gate the scoping skill will feed (`POST /tasks` rejects criteria without commands).
- **2026-08-19** — **`paused` added to the task lifecycle.** The PRD lists `fm pause` but no paused state; treated as a resting state between `ready` and `running`, resumable with `fm run`.
- **2026-08-19** — **Park mode mechanics:** any non-`fyi` `fm ask` parks. The daemon's reply tells the worker to stop; the orchestrator gives it 90s to exit on its own, then interrupts; a handoff brief is acquired from the dying session before teardown; the answer is injected (with the handoff) into the respawn. `fyi` records without parking. On operator answer, the step's attempt counter resets to 1 (human intervened → ladder restarts).
- **2026-08-19** — **Escalation answers:** the failure-ladder escalation question carries options `retry`/`abandon` plus free text; answering `abandon` abandons the task, anything else restarts the step with the answer injected as a binding amendment.
- **2026-08-19** — **Worker timeout is treated like a context wall** (interrupt → handoff → next generation), bounded by `max_generations`; a hung worker therefore relays rather than failing opaquely.
- **2026-08-19** — **Hook callbacks are `fm _event <name> --task ... --url ...` POSTing to the daemon**; values baked into generated scripts (no env inheritance assumptions); on unreachable daemon they append to `<worktree>/.fm/events-fallback.jsonl` and never fail the worker (hook exit 0 always).
- **2026-08-19** — **Daemon stack: FastAPI + uvicorn**, bound to 127.0.0.1 only. Worker `fm ask` allowlisted as `Bash(fm ask:*)` only — workers cannot operate the daemon.
- **2026-08-19** — **Default worker tool allowlist** = Read/Glob/Grep/Edit/Write + `Bash(fm ask:*)`; contracts widen per step (e.g. `Bash(npm test:*)`).
- **2026-08-19** — **Worker prompts pin cwd = worktree.** Smoke run 1 showed a worker "helpfully" writing into the original repo path it saw in the contract; the prompt now forbids that explicitly. (Phase 2's scope guard will also enforce it mechanically.)
- **2026-08-19** — **Wall trigger is orchestrator-side transcript polling + SIGINT, not PreCompact-blocking.** Auto-compaction's window can't go below 100k tokens; our tracker allows arbitrary thresholds. The PreCompact exit-2 hook stays installed as a backstop. Default wall: 150k tokens (config).
- **2026-08-19** — **Handoff acquisition = resume-the-walled-session** (`claude -p --resume <sid> --tools ""`); injection into gen N+1 via SessionStart `additionalContext`.
- **2026-08-19** — **Workers get hook wiring via a First-Mate-owned settings file passed with `--settings`** (regenerated fresh each generation), never by writing into user/repo settings files.
- **2026-08-19** — **Worker permission posture: `--permission-mode dontAsk` + explicit `--allowedTools`.**
- **2026-08-19** — Worker model pinned per invocation (config `worker_model`, per-step override in contract).
- **2026-08-19** — Headless worker baseline context is ~38k tokens — wall thresholds are relative to that, not zero.
- **2026-08-19** — Project memory = `CLAUDE.md` (stable) + `STATUS.md` (mutable state), so the auto-loaded file stays small.
- **2026-08-19** — **Guard architecture: PreToolUse → `fm _guard` reading `<worktree>/.fm/guard.json`**, regenerated every generation from the (possibly amended) contract + config, so operator approvals flow in without hook rewrites. Fail-open only when guard.json is absent (guard not installed); fail-closed with an explanatory message on internal errors — a wrongly-blocked worker asks a question, a silent bypass would be invisible.
- **2026-08-19** — **Guard path semantics:** paths outside the worktree are blocked except scratch prefixes (`/tmp`, `/private/tmp`, `/var/folders`, `/dev`) — and the scratch exemption applies only to paths *outside* the worktree, so a worktree living under /tmp (tests, smokes) still gets full scope enforcement. `.fm/**` is always blocked (orchestration state). Bash write-detection is a deliberate heuristic (redirections, rm/mv/cp/sed -i/tee/…, dependency installers, git push); the per-step tool allowlist remains the primary gate for exotic shell.
- **2026-08-19** — **Amendment semantics for guard questions:** an answer beginning "allow"/"yes" to a `scope_change`/`approval` question mechanically appends `evidence.paths` to `scope_in` (and to `tripwire_allow` when a tripwire was involved); a non-path tripwire approval (git_push, diff thresholds) sets `contract.tripwires[name] = false` for the rest of the task. Any other answer is recorded as a binding amendment only. Abandon-on-answer now applies to `approval` questions too (was decision-only).
- **2026-08-19** — **Diff tripwires run at step boundaries, after validation passes:** the step is marked done first, then the task parks — so an "allow" resume skips straight to the next step instead of re-running finished work. Each diff tripwire is raised at most once per task. Untracked files aren't counted (git diff HEAD --numstat).
- **2026-08-19** — **Per-project tripwire configuration lives in the contract** (scoping sets it), over global defaults in `config.json`; no separate per-project config file.
- **2026-08-19** — **Scoping = interactive `claude "<prompt>"`** (FM-generated prompt, not an installed skill file), run in the repo cwd with a read-only allowlist plus `Write(//<scoping-dir>/**)` and `Bash(fm contract check:*)`; the contract self-check inside the chat is the same `validate_contract` gate POST /tasks enforces. If the daemon is down when the chat ends, the contract is kept and `fm task add <path>` is suggested.

- **2026-08-19** — **Dashboard build artifacts are not committed.** `dashboard/dist` is gitignored (the repo's global `dist/` pattern); `fm serve` looks for it at `<repo>/dashboard/dist` (resolved relative to the daemon source tree, overridable with `FM_DASHBOARD_DIST`) and answers `/` with a build hint when absent. One `pnpm build` after clone is the trade for clean diffs.
- **2026-08-19** — **"Needs you" on the dashboard = every open non-fyi question**, not just `blocking` urgency. Any non-fyi question parks its task (Phase 1 decision), so a normal-urgency question still means a task is waiting on the operator; urgency only governs paging (Slack immediacy, Phase 4). Found live: a parked task showed "nothing needs you" before the fix.
- **2026-08-19** — **Live streaming is dashboard-pull-shaped:** the daemon captures panes only while a WebSocket client is connected (1s cadence, broadcast only on change), so an unwatched daemon does no tmux polling for observability. Pane capture is observability-only — state still flows exclusively through hooks/events (PRD §7 respected; capture code stays in `exec/tmux.py`).
- **2026-08-19** — **Contract edits from the dashboard are blocked while the task runs** (`PUT /tasks/{id}/contract` → 409): between-steps editing per PRD §6.1 is approximated as "no live orchestrator loop"; mid-run amendments happen only through answered questions. Step-state is reconciled on edit (surviving step ids keep their state, new steps start pending, removed steps drop).
- **2026-08-19** — **Open question §10.1 resolved: scoping-in-browser = daemon-mediated headless conversation**, not a PTY. Each operator turn is one `claude -p --resume <sid>` invocation (same scoping prompt/allowlist/`fm contract check` gate as the terminal flow); continuity lives in the Claude Code transcript, the daemon keeps only a thin chat record in `scoping.json`. Turns run inline in the request (409 if one is already running); approval calls the same task-creation path as POST /tasks. Superseded earlier same-day decision to defer.
- **2026-08-19** — **Permission-rule gotcha, load-bearing:** path-scoped file rules must be written `Edit(//abs/path/**)` — `Write(path)` rules are accepted but never consulted (verified live + against docs). Writing outside the session cwd additionally requires `--add-dir`. Any future feature granting scoped file access must use Edit-rules + --add-dir.
- **2026-08-19** — **A task's starting point is an explicit input, chosen before scoping, and pinned.** Jacques: "my current repo state may be messy, I usually pull latest from origin/main, or start from an existing branch — I want each task to pick a clear starting point at the initial input stage." So the worktree moved from first-run (lazy, off `HEAD`) to task-creation time, off an operator-chosen committish resolved to a SHA. Two sub-decisions: (a) **the picker fetches when it opens** — a starting point labelled "latest" that reflects last week's fetch is worse than no label, and `git fetch --prune` only moves remote-tracking refs, so it is safe against a dirty repo; a fetch failure degrades to "not fetched · <reason>" rather than blocking. (b) **starting point and destination stay separate concepts** — the base is where the task begins, the task always commits to its own `fm/<task-id>` branch, so the operator's branches are never moved or dirtied and an abandoned task leaves them untouched. Consequence accepted: the scoping conversation now reads the worktree rather than the operator's checkout, which is the point (it can no longer be confused by uncommitted work) but does mean scoping cannot see work in progress; `--from HEAD` is the escape hatch.
- **2026-08-19** — **A task exists from the first keystroke; `scoping` is a real task status.** Jacques's feedback: the New-task page was a limbo where a conversation happened before anything existed. Resolution: `POST /scoping` creates the Task (status `scoping`, placeholder contract of goal+repo) and the conversation lives in that task's view. Consequences accepted: (a) `validate_contract` is *not* applied to the placeholder — `Store.create_task` never validated, only `POST /tasks` does, and the real gate still runs at approval; (b) an abandoned or never-finished scoping conversation leaves an `abandoned` task row rather than nothing, which is the honest record of an attempt; (c) `scoping` tasks hold no worker slot and `POST /tasks/{id}/run` 409s until a contract is adopted.
- **2026-08-19** — **Scoping turns run off the request path.** A turn takes minutes (the assistant reads the repo); holding the HTTP request open meant the browser could show nothing until it finished, which is what made the old flow feel like limbo. Turns are now `asyncio` tasks (strong-referenced in a set so GC can't cancel them) and the endpoints return immediately, with the operator's own message recorded synchronously. The browser follows over the WS `scoping` frame plus a poll while `thinking`. `POST /scoping/{id}/message` takes `{"wait": true}` for callers that want the old synchronous shape (tests, any future CLI use).
- **2026-08-19** — **Terminal `fm task "<goal>"` stays contract-first, deliberately not task-first.** It is a synchronous PTY conversation; creating a task up front would leave a phantom `scoping` task whenever the operator Ctrl-Cs out of the chat, and the daemon has no way to observe that exit. The asymmetry is intentional: task-first exists because the *browser* flow is asynchronous and the conversation needs somewhere to live.
- **2026-08-19** — **Terminal chat statuses (`approved`/`abandoned`) are sticky against in-flight turns.** Found live: abandoning a task while its first scoping turn was running let the landing turn flip the chat back to `awaiting_operator`. `advance` now re-reads `scoping.json` before writing the post-turn status and yields to a terminal status found there; the reply is still recorded, it just doesn't reopen the conversation.
- **2026-08-19** — **Assistant-authored text renders as markdown, via our own renderer, from React elements only.** Scoping replies, worker questions, handoff briefs and contract `context` are all written by Claude in markdown. `dashboard/src/markdown.tsx` is ~300 lines of the subset Claude actually emits and adds no runtime dependency (the dashboard still ships React and nothing else). It never touches `innerHTML`, so assistant output cannot inject markup; anything unrecognised falls through as text, so it can never silently drop content. Emphasis uses CommonMark-ish flanking because Claude writes `scope_in`/`max_diff_lines` constantly and naive rules ate the underscores. Single newlines render as `<br>` (chat convention), not spaces.
- **2026-08-19** — **The page never scrolls horizontally; wide content scrolls inside its own box.** `.content` carries `overflow-x: hidden` as a hard backstop and every wide surface (diff, code, table, tmux pane) gets its own `overflow-x: auto` container. Rationale: a horizontally scrolling page hides the whole left column, and no amount of per-component care prevents one long token from reintroducing it. Layout that must respond to width lives in CSS, not inline styles — an inline `display` beats a media-query class (found live: the sidebar footer survived the narrow-width collapse).
- **2026-08-19** — **`.fm/` and build caches are excluded from the operator's diff and from diff-tripwire totals** (`gitops.is_review_noise`). First Mate's own per-worktree state is orchestration, not the work under review; counting it would also charge the operator's `max_diff_lines` budget for our hook scripts. Filtering lives in `exec/gitops.py`, keeping the "only gitops touches git" boundary intact.
- **2026-08-19** — **`uvicorn[websockets]` class of bug: real-transport checks are required for transport features.** The `/ws` 404 was invisible to FastAPI's TestClient; live browser verification caught it. Phase 4+ features with real transports (Slack socket mode) should get at least one real-connection smoke.

## Open questions

Carried from PRD §10 — raise with Jacques when they become blocking; otherwise record an assumption above and proceed:

1. Scoping-in-browser mechanics (deferred by terminal-first Phase 2).
2. Multi-repo tasks — v1 may declare single-repo only.
3. Cost controls (per-task budget) — v1 or Phase 5? (`--max-budget-usd` exists on `claude -p`; cheap to wire per-worker.)
4. Dashboard auth — localhost-only assumed for v1 (daemon already binds 127.0.0.1 only).

## Session log

One dated entry per working session: who/what/outcome, newest first.

- **2026-08-19** — Session 6 addendum (Claude, on Jacques's feedback: "create the worktree at scoping time instead, the user selects which branch they want to start from"): built the starting-point picker end to end — `GET /fs/refs` (fetch + ranked candidates with freshness), `base` on `POST /scoping` and `POST /tasks`, worktree cut at task creation, `Task.base`/`base_sha` pinned, scoping conversation reads the worktree, `fm task --from REF`, cleanup of untouched worktrees on abandon, and `StartPoint.tsx` in the New-task form. Live-verified against a purpose-built messy repo (stale local main, unfetched colleague commit, uncommitted syntax error, old feature branch): worktree came from the fresh remote tip, the operator's tree was untouched, task reached `done` 3/3. Two UI refinements came out of looking at real output: the redundant "remote default · remote" pair, and `fm/**` task branches piling up as fake choices. 107 daemon tests, 13 dashboard tests, phase-2 smoke, and the 80-check responsive sweep all pass.
- **2026-08-19** — Session 6 (Claude, on Jacques's hands-on feedback round 1): three reported problems, all fixed and live-verified. (1) **Task-first scoping** — starting a task now creates the session immediately (`scoping` status) and the conversation runs inside its task view; turns moved off the request path; approval adopts the contract into that same task (`Store.adopt_contract`) instead of creating a second one; run/abandon/boot-reconcile guards added. (2) **Markdown rendering** — new dependency-free `markdown.tsx` used for scoping replies, questions, handoff briefs and contract context; 13 vitest tests (`pnpm test` is new). (3) **Responsive** — no horizontal page scroll anywhere; verified with 80 headless-Chromium checks (5 views + 5 task tabs × 8 widths, 1600px→420px). Live end-to-end run: repo picker → session → real scoping conversation → approve and run → worker → `done` with 2/2 criteria, exactly one task the whole way. Bugs found by testing, not by reading: the abandon-vs-in-flight-turn race, the table-after-paragraph parse, intra-word emphasis eating identifiers, the sidebar footer's inline `display` beating the collapse rule, `.fm/` polluting the diff view (13 files → 2), and `pnpm dev` missing `/scoping` + `/fs` proxies. 99 daemon tests + 13 dashboard tests pass; phase-2 smoke passes. NOT done: Jacques's visual re-check; the terminal `fm task` flow is deliberately left contract-first (see decision log).
- **2026-08-19** — Session 5 addendum (Claude, on Jacques's feedback "repo + task input first, simple UI, before the scope querying"): rebuilt the New task flow — repo picker (suggestions + fs browser over new daemon endpoints) + goal → scoping chat in the dashboard (`scoping_api.py`, headless `claude -p --resume` turns) → approve-and-run. Live-verified end to end with a real scoping conversation and a real worker run to `done` (6/6 criteria). Found and fixed the Edit-vs-Write permission-rule trap (with --add-dir) that silently broke contract writing in BOTH scoping flows; hardened the scoping prompt against out-of-allowlist step instructions. 94 tests pass. Committed.
- **2026-08-19** — Session 5 (Claude): built Phase 3 end to end — `dashboard/` SPA (React/TS, prototype-faithful: amber-accent dark theme, mono/sans split, glyphs, generation rail with handoff briefs), daemon API additions (diff + per-file diff, on-demand output capture, memory CRUD, contract PUT with step-state reconciliation, context meters in status/detail, live WS capture loop, static `/ui` mount, `fm serve --open`). Fixed a latent Phase-1 bug (uvicorn without a websockets library → `/ws` 404 over real HTTP). 91 tests pass. Live-verified with three real worker tasks driven through headless Chromium against the built SPA: park → answer-in-browser → resume → done; failure ladder exercised organically; live stream/context meter captured mid-run; zero console errors. Two UX fixes came out of live runs: needs-you now shows all open non-fyi questions, and stale live payloads are ignored once a task settles. NOT done: Jacques's visual pass (Phase 3 closure), scoping-in-browser (deferred, §10.1). Next: Phase 4.
- **2026-08-19** — Session 4 addendum (Claude + Jacques): Jacques confirmed the prototype replacement was his intentional UI/UX improvement and committed it (`prototype/First Mate System Prototype.html` + `support.js` supersede `dashboard.html`); README/CLAUDE.md references updated. **Phase 2 formally closed by Jacques.** Live scoping-chat run moved to the carried nice-to-have list.
- **2026-08-19** — Session 4 (Claude): built Phase 2 end to end — `guard.py` scope-guard engine + `fm _guard` PreToolUse hook (wired via `workerfiles`, config compiled per generation), tripwires (manifests/migrations/push in the hook; diff thresholds in the orchestrator at step boundaries), mechanical scope-widening on "allow" answers, `scoping.py` + `fm task "<goal>"` interactive scoping with `fm contract check` self-validation, contract fields `tripwires`/`tripwire_allow` + validation. 86 tests pass (42 new). Live-verified the hook wiring with a real headless session (blocked in-band, event logged; also caught+fixed a temp-prefix bypass the unit tests missed). Phase-2 smoke (`fm-smoke --scenario phase2`) passed first try: block → park → allow → widened scope → resume → done. NOT committed: unexplained working-tree changes under `prototype/` (dashboard.html deleted, an exported HTML added) — left for Jacques to confirm. Next: Phase 3 dashboard; Jacques should also run one live `fm task "<goal>"` scoping chat.
- **2026-08-19** — Session 3 (Claude): built Phase 1 orchestrator core end to end — models/store (files + rebuildable SQLite index), shell validation with evidence, worker hook files (`fm _event` → daemon), orchestrator loop (relay, park mode, failure ladder, reconciliation), FastAPI daemon (REST + WS + worker-slot cap), full `fm` CLI, 33 new unit tests (44 total, all passing), `fm-smoke` e2e. Smoke passed twice with real sonnet workers (second run clean after pinning cwd=worktree in the worker prompt); park→answer→resume and the failure ladder both exercised for real. Phase 1 exit criteria met. Next session: Phase 2 item 1 (scoping skill) or item 2 (scope-guard hook) — they're independent.
- **2026-08-19** — Session 2 (Claude): verified Claude Code facts against live CLI/docs; scaffolded `daemon/` (four exec modules, spawner, 11 tests); built and **passed the Phase 0 relay spike** — 12/12 correct across 7 generations, exit criterion met, design is GO. Artifacts under `~/.firstmate/spike/run-20260819-130152/`.
- **2026-08-19** — Session 1: created the multi-session memory system (`CLAUDE.md`, `STATUS.md`); initialized git and pushed the public repo. No application code written.
